GOOGLE_PROJECT_ID=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=global
GOOGLE_STT_LOCATION=us-central1
SERPER_API_KEY=your-serper-api-key
PEXELS_API_KEY=your-pexels-api-key

# Additional media libraries. Each is optional: a provider without its key
# reports that it needs one and is skipped, rather than failing the run.
# Wikimedia Commons needs no key at all.
PIXABAY_API_KEY=your-pixabay-api-key
UNSPLASH_ACCESS_KEY=your-unsplash-access-key
# Unsplash requires attribution links to carry your app's utm source.
UNSPLASH_APP_NAME=video_factory

# ── Media aggregator: open and archival sources ─────────────────────────────
# These reach the correctly identified pictures of real subjects that stock
# libraries do not have -- a named player, a specific stadium, a dated event,
# a genuine satellite view.
#
# No key needed, already working: Wikimedia Commons, Internet Archive,
# Library of Congress, The Met Open Access, NASA Image and Video Library,
# NOAA (via its Commons-hosted files), and Openverse anonymously.
#
# Optional keys below. Every one is optional: without it that single source
# reports itself unavailable and the router uses the others. Nothing is
# blocked and no generation fails.

# Openverse works without this; a token only raises the rate limit.
# Register: https://api.openverse.org/v1/auth_tokens/register/
OPENVERSE_API_TOKEN=

# Smithsonian Open Access. Free api.data.gov key, issued instantly.
# Sign up: https://api.data.gov/signup/
SMITHSONIAN_API_KEY=

# Europeana. Free key for the European cultural heritage collections.
# Request: https://pro.europeana.eu/pages/get-api
EUROPEANA_API_KEY=

# Research providers. Both return reference material only -- neither offers a
# file this pipeline may republish.
YOUTUBE_API_KEY=your-youtube-data-api-v3-key
NEWSAPI_KEY=your-newsapi-key

# Optional overrides
GEMINI_PRIMARY_MODEL=gemini-2.5-pro
GEMINI_REVIEW_MODEL=gemini-3-flash-preview
GEMINI_TTS_MODEL=gemini-2.5-pro-tts
REMOTION_REQUIRE_GPU=false
GEMINI_IMAGE_MODEL=gemini-2.5-flash-image

# ── AI gateway (OmniRoute) ────────────────────────────────────────────────
# `direct` keeps the existing behaviour: every model call goes straight to
# Gemini. `omniroute` sends chat completions through an OpenAI-compatible
# gateway instead. Image generation, web-grounded research and Gemini TTS
# always stay direct regardless of this setting.
AI_GATEWAY_MODE=direct
AI_GATEWAY_BASE_URL=
AI_GATEWAY_API_KEY=

# Feature flags. Defaults preserve current behaviour; turn routing on to let
# the task policy pick a cheaper model where the task allows it.
AI_SMART_ROUTING_ENABLED=false
AI_CACHE_ENABLED=true
AI_REQUEST_COALESCING_ENABLED=true
